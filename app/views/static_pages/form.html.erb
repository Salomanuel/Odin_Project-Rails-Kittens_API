<h1>Search Images on Flickr</h1>
<p>the form is slow because Flickr's API is not incredibly fast, so have some patience</p>

<%= form_tag('/form', method: :get) do %>
		<%= label_tag(:name) %>
		<%= text_field_tag(:name) %>
		<%= submit_tag("search") %>
<% end %>

<% if @user %>
	<% if user_exists?(@user) %>
		<p>displaying pictures for user <strong><%= @user %></strong></p>
		<% user_photos(@user).each do |p| %>
			<%= link_to(image_tag(get_url(p),
								:class 	=> 'flickr_photo', 
								:title 	=> p.title, 
								:border => 0, 
								:size 	=> '200'),
							 get_url(p)) %>
		<% end %>
	<% else %>
		<p>User not found</p>
	<% end %>
<% end %>
<br>